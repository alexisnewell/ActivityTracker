<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activitytracker">

    <!--
        BODY_SENSORS lets us read accelerometer/gyroscope.
        ACTIVITY_RECOGNITION is required on Android 10+ for step-counter hardware sensor.
    -->
    <uses-permission android:name="android.permission.BODY_SENSORS"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <!-- Declare that we USE an accelerometer (not required but good practice) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true"/>
    <uses-feature android:name="android.hardware.sensor.gyroscope"     android:required="false"/>

    <application
        android:label="Activity Tracker"
        android:theme="@style/Theme.ActivityTracker">

        <activity
            android:name="com.example.activitytracker.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".workouts.WorkoutActivity" />

    </application>
</manifest>
